/** 
 * smallest substring
 * find the smallest substring of all characters
 * return empty string if such a substring doesn't exist
 *
 * input 
 * arr = [x,y,z]
 * str = "xyyzyzyx"
 *
 * arr = ""
 * arr not found in str
 *
 * Map
 * {
 * 	// x : [xyy, zyx]
   	// y : [yyz, yzy, yzy, zyx]
   	// z : [zyz, zyx]
 * }
 * 
 *
 * */

function getShortestUniqueSubstring(arr, str) {
	let arrMap = new Map();

}
